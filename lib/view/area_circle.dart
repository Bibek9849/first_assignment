import 'package:flutter/material.dart';

class CircleAreaView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Area of Circle')),
      body: Center(
        child: Text('Calculate Area of Circle Here!'),
      ),
    );
  }
}
